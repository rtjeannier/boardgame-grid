# Python
__pycache__/
*.pyc
.venv/

# Node / Vite
web/node_modules/

# Cached BGG API responses (regenerated on demand)
data/cache/

# The built site (docs/) and web/public/grid.json ARE committed on purpose:
# GitHub Pages serves docs/, and the grid JSON is the reviewable output.
